"use client";
import Link from "next/link"; import { ArrowDown, Instagram, Linkedin, Youtube } from "lucide-react"; import { useRef,useState } from "react"; import { Navigation } from "./Navigation"; import { site } from "@/content/site";
export function Hero(){const ref=useRef<HTMLVideoElement>(null);const[muted,setMuted]=useState(true);const sound=async()=>{if(!ref.current)return;ref.current.muted=!muted;setMuted(!muted);try{await ref.current.play()}catch{}};
return <section className="hero" id="top"><Navigation muted={muted} onSound={sound}/><video ref={ref} className="heroVideo" autoPlay muted={muted} loop playsInline preload="metadata" poster={site.hero.poster}><source src="/videos/hero.mp4" type="video/mp4"/></video><div className="heroShade"/><div className="heroCopy"><p className="eyebrow">{site.hero.eyebrow}</p><h1>{site.hero.title}</h1><div className="actions"><Link className="btn light" href="#about">Discover Adhvan</Link><Link className="btn ghost" href="#media">Watch Reveal</Link></div></div><Link className="scrollCue" href="#about"><span>Scroll to discover</span><ArrowDown size={17}/></Link><div className="social"><a href="#" aria-label="LinkedIn"><Linkedin/></a><a href="#" aria-label="YouTube"><Youtube/></a><a href="#" aria-label="Instagram"><Instagram/></a></div></section>}
